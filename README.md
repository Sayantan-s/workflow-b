> Talk about the Run Preview Feature

- Execution Trace
- Execution Context

> Undo redo

- Dual Stack Model
- Expression Preview
- why choose localstorage over indexdb

> State Shape;

Used normalization for faster IO.

Operation Array (Before) Map (After)<br />
Add node O(1) O(1)<br />
Delete node O(n) O(1)<br />
Find node O(n) O(1)<br />
Update node O(n) O(1)<br />
Add edge O(1) O(1)<br />
Delete edge O(n) O(1)<br />
Find edge O(n) O(1)
